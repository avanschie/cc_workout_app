# Environment Configuration Example
# Copy this file to .env.staging, .env.local, or .env.production and fill in your values

# Supabase Configuration
# Get these from your Supabase project settings > API
# Use the "API keys" section (not "legacy API keys")
# URL: Your project's API URL
# ANON_KEY: The "publishable key" (anon/public role)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-publishable-key-here

# Note: Never commit actual credentials to git!
# The .env.* files are ignored by .gitignore to keep your secrets safe.